# 🎁 Telegram Mini App - Scratch Card

Интерактивная скретч-карточка для Telegram Mini App. Пользователь стирает защитный слой, чтобы увидеть скрытый контент.

## Возможности

- ✨ Реалистичный эффект стирания с Canvas API
- 📱 Полная поддержка touch-событий для мобильных устройств
- 🎨 Адаптивная тема под цвета Telegram
- 💫 Haptic feedback при взаимодействии
- 🎉 Эффект конфетти при полном открытии
- 📊 Индикатор прогресса

## Структура проекта

```
Gift/
├── index.html    # Основной HTML файл
├── styles.css    # Стили
├── script.js     # Логика scratch card
└── README.md     # Документация
```

## Как использовать

### 1. Разместить на хостинге

Загрузите файлы на любой хостинг с HTTPS (обязательно для Telegram Mini Apps).

### 2. Создать бота в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Отправьте `/newapp` чтобы создать Mini App
4. Укажите URL вашего хостинга

### 3. Изменить контент

Чтобы изменить контент под скретч-слоем, отредактируйте блок в `index.html`:

```html
<div class="placeholder-content">
    <span class="gift-emoji">🎉</span>
    <p class="gift-text">Здесь будет твой подарок!</p>
</div>
```

Или используйте JavaScript функцию:

```javascript
setGiftContent(`
    <div style="text-align: center;">
        <h2>Поздравляем!</h2>
        <p>Вы выиграли скидку 50%!</p>
    </div>
`);
```

## Настройки

В файле `script.js` можно изменить параметры:

```javascript
new ScratchCard('scratch-canvas', {
    brushSize: 45,        // Размер кисти для стирания
    revealThreshold: 55,  // % стирания для автоматического открытия
});
```

## Локальная разработка

Для тестирования запустите локальный сервер:

```bash
# Python 3
python -m http.server 8080

# Node.js (npx)
npx serve .
```

Откройте http://localhost:8080 в браузере.

